# Shared Common Dependencies
# These are imported by all microservices

# Pydantic for data validation
pydantic>=2.10.0
pydantic-settings>=2.6.0

# HTTP and async utilities
httpx>=0.28.0
aiofiles>=24.1.0

# Configuration and environment
python-dotenv>=1.0.0

# Logging and monitoring
structlog>=24.4.0
python-json-logger>=3.1.0

# Security
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
cffi>=1.17.0  # Required for cryptography on Python 3.14
cryptography>=43.0.0  # Python 3.14 compatible
PyJWT>=2.9.0  # JWT token decoding (Python 3.14 compatible)

# OpenTelemetry Core (shared across services)
opentelemetry-api>=1.28.0
opentelemetry-sdk>=1.28.0